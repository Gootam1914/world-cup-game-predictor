{
  "name": "soccer-game-predictor",
  "version": "1.0.0",
  "private": true,
  "description": "International football match result predictor (XGBoost + StatsBomb). The model runs on Python; these npm scripts just start it for you.",
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "start": "bash scripts/dev.sh",
    "setup": "bash scripts/dev.sh --setup-only",
    "build": "bash -c 'source .venv/bin/activate 2>/dev/null; python scripts/build_all.py'"
  }
}
